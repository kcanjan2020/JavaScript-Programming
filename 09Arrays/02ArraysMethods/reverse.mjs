// *** reverse() method ******
/* => All method return something but push,pop,shift and unshift change the original array
=>While reverse and sort method do both return and change original array */

let arr1 = [0, 2, 3, 4, 11, 10, 20, 21, 31, 1];
let revArray = arr1.reverse();
console.log(arr1);
console.log(revArray);

let strArr = ["e", "a", "b", "c", "d"];
let revStrArray = strArr.reverse();
console.log(strArr);
console.log(revStrArray);
